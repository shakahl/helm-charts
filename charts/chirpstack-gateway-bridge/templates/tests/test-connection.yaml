apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chirpstack-gateway-bridge.fullname" . }}-test-connection"
  labels:
    {{- include "chirpstack-gateway-bridge.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chirpstack-gateway-bridge.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
