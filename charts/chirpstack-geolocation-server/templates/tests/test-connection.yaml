apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chirpstack-geolocation-server.fullname" . }}-test-connection"
  labels:
    {{- include "chirpstack-geolocation-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chirpstack-geolocation-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
